ALTER TABLE API ADD COLUMN CODE VARCHAR(100);

UPDATE API SET CODE = LOWER(REPLACE(NAME, ' ', '-'));

ALTER TABLE API ALTER COLUMN CODE SET NOT NULL;

ALTER TABLE API ADD CONSTRAINT UK_API_CODE UNIQUE (CODE);